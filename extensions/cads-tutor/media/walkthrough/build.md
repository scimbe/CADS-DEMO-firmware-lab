# Firmware bauen / Build the firmware

- Terminal → **Run Build Task** (Ctrl/Cmd+Shift+B) startet `CaDS: Build` (`cmake --preset itsboard && cmake --build --preset itsboard`).
- Artefakte: `build/itsboard/cads-zero.{elf,bin,hex}`.
- Fehler erscheinen im Terminal und in der Problems-Ansicht.

---

- Terminal → **Run Build Task** (Ctrl/Cmd+Shift+B) runs `CaDS: Build`.
- Artifacts: `build/itsboard/cads-zero.{elf,bin,hex}`.
- Errors show up in the terminal and in the Problems view.
