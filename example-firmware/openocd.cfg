# Default target: Nucleo-F401RE's onboard ST-Link/V2-1 + STM32F401RE.
# Override by editing this file (or bind-mounting a different one at /workspace/openocd.cfg)
# if your board differs -- e.g. swap target/stm32f4x.cfg for target/stm32f1x.cfg for a
# "Blue Pill" STM32F103 + external ST-Link/V2 probe.
source [find interface/stlink.cfg]
source [find target/stm32f4x.cfg]
