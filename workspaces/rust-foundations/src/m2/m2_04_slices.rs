//! m2-04-slices: references to a contiguous part of a collection.

/// Returns the first word of `s` – everything up to the first space, or the
/// whole string if it contains no space (Listing 4-9 in the book).
pub fn first_word(s: &str) -> &str {
    todo!()
}

/// Returns the last word of `s` (after the last space). For a string
/// without spaces, the whole string.
pub fn last_word(s: &str) -> &str {
    todo!()
}

/// Returns the sum of all elements of the slice.
pub fn sum(xs: &[i32]) -> i32 {
    todo!()
}

/// Returns the slice without its first element; the empty slice stays empty.
pub fn tail(xs: &[i32]) -> &[i32] {
    todo!()
}
