// m3-01-for-and-while
// countUp(n)      -> [1, 2, ..., n];  countUp(0) is []
// sumUntil(list, stop) -> the sum of the numbers BEFORE the first element that
//                         is >= stop. sumUntil([1, 2, 9, 3], 9) is 3.
// Write one classic `for` loop and one `while` loop. Both currently throw.

import "../course-hint.js"; // prints guidance if this file is run directly

export function countUp(n) {
  throw new Error("TODO: build the array with a for loop");
}

export function sumUntil(list, stop) {
  throw new Error("TODO: add numbers with a while loop until one reaches stop");
}
