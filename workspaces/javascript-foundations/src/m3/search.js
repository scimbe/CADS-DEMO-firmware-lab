// m3-04-break-continue
// stripComments(lines) -> the lines that are neither empty nor start with "#"
//                         (use `continue`)
// findInGrid(grid, target) -> {row, col} of the first occurrence, scanning row
//                         by row, or null. Both loops must stop at the hit -
//                         a labeled `break` leaves both at once.

export function stripComments(lines) {
  throw new Error("TODO: skip empty and commented lines with continue");
}

export function findInGrid(grid, target) {
  throw new Error("TODO: scan the grid and leave both loops at the first hit");
}
