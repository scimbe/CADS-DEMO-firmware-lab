// PLACEHOLDER starter module for the CaDS Tutor `javascript-foundations`
// course (see PLACEHOLDER.md). Each step has a test file under exercises/.

/** Step m0-01: a first function with a test. */
export function greet(name) {
  return `Hello, ${name}!`;
}

/** Step m0-02: strict equality warm-up – returns true only for the same type and value. */
export function isSame(a, b) {
  return a === b;
}
